export enum UserRole {
  ADMIN = 'admin',
  CUSTOMER = 'customer',
}

export enum EndPoint {
  SIGNUP = 'signup',
  LOGIN = 'login',
}

export enum LinkType {
  SideBar = 'SideBar',
  Menubar = 'MenuBar',
}
